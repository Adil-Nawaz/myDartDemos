void main() {
/* Q1. Write a program having two integer variables and check whether 
 * they are equal or not and print the answer on console. 
 * For example if a=10, b=10 then both ‘a’ & ‘b’ are equal */
  int var1 = 10, var2 = 10;
  (var1 == var2) ? print("Both integers are equal") : print("integers are not equal");
  
/* Q2. Write a program to find a greater number between two integer variables and
 * print the answer on console. For example if a=20, b=15 then a is the greatest */
  int var3 = 20, var4 = 20;
  (var3 > var4) ? print("$var3 is greater than $var4") : print("$var3 is not greater than $var4");
 
/* Q3. Write a Program to check whether a person is eligible to vote or not. For example, 
 * if age is less than 18 then he/she wouldn’t be able to cast his/her vote. */
  int age = 18;
  if (age < 18)
  {
    print("You are not eligible to cast vote");
  } else if ((age == 18) || (age > 18))
  {
    print("You are eligible to cast vote");
  } else 
  {
    print ("Not defined");
  }
  
/* Q4. Write a Program to find the largest number of the three and print the answer on console. 
 * For example, if a=10, b=20, c=30 find the largest number among them */
  int num1 = 1, num2 = 0, num3 = 2;
  if (num1 > num2 && num1 > num3)
  { 
    print("$num1 is greater than $num2 & $num3");
    }
   else if (num2 > num1 && num2 > num3)
  { 
    print("$num2 is greater than $num1 & $num3");
    }
  
  else if (num3 > num1 && num3 > num2)
  { 
    print("$num3 is greater than $num1 & $num2");
    }
   else {
    print ("Not defined");
  }
  
 /* Q5. Write a program to check whether a number is negative, positive or zero and print the 
  * answer on console. If a=10 then it will print “Number is positive”. */
  int number = -9;
  if (number < 0)
  {
    print("Number is negative");
  } else if (number > 0)
    {
      print("Number is positive");
    } else 
  {
    print ("Number is 0");
  }
  
 /* Q8. Student Grading System
Build a “Student Grading System” using the concept of if-else statement. If marks of a 
student is equal to or greater than 90 then he will get ‘A+’ grade, if he gets marks in between 
80-89 then the grade will be A, if marks in between 70-79 then grade will be ‘B’ and students 
having marks less than 70 will get ‘F’ grade. */
  int marks = 69;
  if (marks == 90 || marks > 90)
  {
    print("A+ Grade");
  }
  else if (marks <90 && marks >= 80 )
  {
   print("A Grade");
  }
  else if (marks <80 && marks >= 70)
  {
    print("B Grade");
  }
  else {
    print("F Grade");
  }
  
}
